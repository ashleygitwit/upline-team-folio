{
  "name": "upline-team-folio",
  "private": true,
  "scripts": {
    "generate-plan": "node scripts/generate-execution-plan.mjs",
    "sync-data": "cp data/venture-plan.json internal-comms/public/data/venture-plan.json && cp data/learnings.json internal-comms/public/data/learnings.json",
    "prepare": "npm run generate-plan && npm run sync-data",
    "dev": "npm run prepare && npm --prefix internal-comms run dev",
    "build": "npm run prepare && npm --prefix internal-comms run build",
    "preview": "npm run prepare && npm --prefix internal-comms run preview"
  }
}
